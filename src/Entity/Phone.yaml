App\Entity\Phone:
    collectionOperations:
        get: # nothing more to add if we want to keep the default controller
            normalization_context: {groups: ['phones_list']}
    itemOperations:
        get: ~
